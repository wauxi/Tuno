MYSQL_ROOT_PASSWORD=musicboard_root
MYSQL_DATABASE=musicboard
MYSQL_USER=music_user
MYSQL_PASSWORD=music_pass

PHP_PORT=8080
VITE_DEV_SERVER_PORT=5173

# Base URL that the frontend will use for API calls (points to php container port)
API_BASE_URL=http://localhost:${PHP_PORT}

# Optional overrides for PHP app
DB_HOST=mysql
DB_PORT=3306

# Enable verbose PHP logging inside the container
PHP_DISPLAY_ERRORS=1

